name: geolocation
description: Location plugin for iOS and Android.
version: 1.1.0
homepage: https://github.com/loup-v/geolocation/

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  streams_channel: ^0.2.3

flutter:
  plugin:
    androidPackage: app.loup.geolocation
    pluginClass: GeolocationPlugin
